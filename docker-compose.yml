version: '3.8'

services:
  frontend:
    build:
      context: ./web/frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./web/frontend:/app
      # Exclude node_modules from volume mounting
      - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000
      - NEXT_PUBLIC_BROWSER_API_URL=http://localhost:8000
    networks:
      - app-network
    depends_on:
      - backend

  backend:
    build: 
      context: ./web/backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./web/backend:/app
    environment:
      - MONGODB_URI=mongodb+srv://goodgodgeorge:cXNSvmHkIOsHjUt4@autoshortsdb.f7asv.mongodb.net/?retryWrites=true&w=majority&appName=autoshortsdb
      - R2_ACCOUNT_ID=68b1765fe971ce074e1a3bad853b4031
      # R2 credentials (updated with new token values)
      - CLOUDFLARE_R2_ACCESS_KEY_ID=da9b059548252aba175a3464f1f3926a
      - CLOUDFLARE_R2_SECRET_ACCESS_KEY=9c3aa5ce21a1ff09c3e63a8e4bd7c81bfd4c383eeb5edc1f8650731ca301d344
      - CLOUDFLARE_R2_ENDPOINT=https://68b1765fe971ce074e1a3bad853b4031.r2.cloudflarestorage.com
      # R2 bucket name (explicitly set for multiple methods of access)
      - R2_BUCKET_NAME=autoshorts-media
      - BUCKET_NAME=autoshorts-media
      - CLOUDFLARE_R2_BUCKET_NAME=autoshorts-media
      # AWS environment variables for boto3 - match R2 credentials
      - AWS_ACCESS_KEY_ID=da9b059548252aba175a3464f1f3926a
      - AWS_SECRET_ACCESS_KEY=9c3aa5ce21a1ff09c3e63a8e4bd7c81bfd4c383eeb5edc1f8650731ca301d344
      - AWS_DEFAULT_REGION=auto
      # Cloudflare credentials for Wrangler
      - CLOUDFLARE_ACCOUNT_ID=68b1765fe971ce074e1a3bad853b4031
      - CLOUDFLARE_API_TOKEN=KN7W8QlwW8NbUW0yxMSo77DwHZMfytl07zST5vS9
      # Set to false to use Cloudflare R2 (not mock storage)
      - USE_MOCK_STORAGE=false
      # ElevenLabs Configuration
      - ELEVENLABS_API_KEY=sk_2661fc8844c2ffc280fb47b44b061d425c156ce09a47a9dc
      - ELEVENLABS_MODEL_ID=eleven_monolingual_v1
      - ELEVENLABS_API_URL=https://api.elevenlabs.io/v1
    networks:
      - app-network

  browser-tools:
    image: node:20-slim
    command: sh -c "npm install -g @agentdeskai/browser-tools-server && npx browser-tools-server --command-port 3025"
    ports:
      - "3025:3025"
    volumes:
      - ./screenshots:/screenshots
    networks:
      - app-network

networks:
  app-network:
    driver: bridge 